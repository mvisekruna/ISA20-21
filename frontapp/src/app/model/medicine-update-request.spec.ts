import { MedicineUpdateRequest } from './medicine-update-request';

describe('MedicineUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new MedicineUpdateRequest()).toBeTruthy();
  });
});
