import { UpdatePricelistRequest } from './update-pricelist-request';

describe('UpdatePricelistRequest', () => {
  it('should create an instance', () => {
    expect(new UpdatePricelistRequest()).toBeTruthy();
  });
});
