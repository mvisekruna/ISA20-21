import { AddWorktimeRequest } from './add-worktime-request';

describe('AddWorktimeRequest', () => {
  it('should create an instance', () => {
    expect(new AddWorktimeRequest()).toBeTruthy();
  });
});
