import { AddOrderItemRequest } from './add-order-item-request';

describe('AddOrderItemRequest', () => {
  it('should create an instance', () => {
    expect(new AddOrderItemRequest()).toBeTruthy();
  });
});
